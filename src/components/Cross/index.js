import * as React from 'react';

const Cross = (): React.Node => {
    const Styles = {
        container: {}
    }
    return(
        <div style={Styles.container}>
        X
        
        </div>
    )
}

export default Cross;